using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Linq;

namespace CSharpConcurrency
{
    [TestClass]
    public class X_PLINQ
    {
        //[TestMethod]
        //public void RENAME_TEMP()
        //{
        //    var integers = Enumerable.Range(0, 1000);
        //    var squares = integers.AsParallel().Select(i => i * i).ToList();
        //}
    }
}
